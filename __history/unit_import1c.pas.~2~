unit unit_import1c;

interface

type
  TDivisionImport1C = class
    id: string;
    pid: string;
    name: string;
    constructor Create(id, pid, name);
  end;

implementation

{ TDivisionImport1C }

constructor TDivisionImport1C.Create(id, pid, name);
begin
  self.id := id;
  self.pid := pid;
  self.name := name;
end;

end.
